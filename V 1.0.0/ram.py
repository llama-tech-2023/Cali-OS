import mainSettings as setting
import id
#import cpu
#import desktop

#universal things
#user = desktop.User

#desktop and user stuff
userbypass = setting.true

#terminal
terminalStatus = 0
currentDirectory = str(" ")
#prompt = str(user) + "@Cali/" + str(currentDirectory) + "/ " + "~$: "
command = setting.null
sudoCommandModifier = setting.null
chosenDir = setting.null

#command process to code
